scalar Date

type User {
  uid: String
  email: String
  username: String
  joined_at: Date
  access_level: Int
}

type Query {
  user(uid: String!): User
  users: [User]
}

type Mutation {
  deleteUser(uid: String!): String #삭제한 사용자 uid
}
